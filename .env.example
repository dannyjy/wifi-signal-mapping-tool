PORT=3000
SESSION_SECRET=wifi_mapping_tool_juba_secret_key_2026

# Database Configuration
# Options: 'sqlite3' (zero-config local fallback) or 'mysql'
DB_CLIENT=sqlite3
DB_FILENAME=./database/wifi_mapping.sqlite3

# MySQL Configuration (Used when DB_CLIENT=mysql)
DB_HOST=127.0.0.1
DB_PORT=3306
DB_USER=root
DB_PASSWORD=
DB_NAME=wifi_mapping_db
